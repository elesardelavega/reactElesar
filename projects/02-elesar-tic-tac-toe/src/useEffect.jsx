import { useEffect, useState } from "react";

const Component = () => {

    const [value, setValue] = useState(false)

    useEffect(
        // function que se ejecuta una vez
        // código a ejecutar cada vez que se renderice el componente
        // (codeToExecute)
        //, 
        // opcional (listOfDependencies)
    )

}